# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

GET     /subscribe                  im.actor.push.controllers.SubscriberController.subscribe()
GET    /push/:channel              im.actor.push.controllers.PushController.send(channel: String)

# GET     /                           im.actor.push.controllers.Application.index()
